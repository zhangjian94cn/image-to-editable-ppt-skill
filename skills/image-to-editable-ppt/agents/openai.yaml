interface:
  display_name: "Image to Editable PPT"
  short_description: "Rebuild images, PDFs, or image-based PPT/PPTX files into editable PPTX"
  default_prompt: "Use $image-to-editable-ppt to rebuild the supplied slide image into an object-level editable page.pptx in the current task directory."
